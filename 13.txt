Write an application program to demonstrate data (state and props) in
React.
npm create vite@latest state-props-demo
cd state-props-demo
npm install
npm run dev

import React, { useState } from "react";

// Child Component (receives props)
function Student(props) {
    return (
        <div>
            <h2>Student Details</h2>
            <p>Name: {props.name}</p>
            <p>Age: {props.age}</p>
        </div>
    );
}

// Parent Component
function App() {
    // State
    const [name, setName] = useState("Ravi");
    const [age, setAge] = useState(20);

    return (
        <div>
            <h1>React State and Props Demo</h1>

            <button onClick={() => setName("Kiran")}>
                Change Name
            </button>

            <button onClick={() => setAge(age + 1)}>
                Increase Age
            </button>

            {/* Passing State as Props */}
            <Student name={name} age={age} />
        </div>
    );
}

export default App;